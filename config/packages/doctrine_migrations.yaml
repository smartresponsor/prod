doctrine_migrations:
    migrations_paths:
        # Host application migrations only.
        # Component migrations must be imported through installed packages/bundles
        # or added explicitly after their vendor paths are verified.
        'DoctrineMigrations': '%kernel.project_dir%/migrations'
    enable_profiler: false
